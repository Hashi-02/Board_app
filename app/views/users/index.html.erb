
<div class="index0">
<div class="wrapper">
    <h2>ユーザー一覧</h2>
    <% @users.each do |user| %>
    
          <%= link_to(user.name, "/users/#{user.id}",class: 'a') %>
          <br>
         
    
    <% end %>
</div>
</div>
